/** Stack sans-serif partagée — pilotée par `next/font` (Inter) dans `app/layout.tsx`. */
export const FONT_SANS_STACK =
  'var(--font-inter), system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';

/** Chiffres tabulaires — IBM Plex Mono (prix, %, KPI). */
export const FONT_NUM_STACK =
  'var(--font-ibm-plex-mono), ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace';
